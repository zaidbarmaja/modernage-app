# دليل تشغيل تطبيق «عصر الحداثة» مع Firebase

التطبيق مبني بالكامل، ويبقى فقط ربطه بمشروع Firebase الخاص بك حتى تعمل قاعدة
البيانات وتسجيل الدخول ورفع الصور والإشعارات. اتبع الخطوات بالترتيب.

---

## 1) إنشاء مشروع Firebase
1. ادخل إلى: https://console.firebase.google.com
2. اضغط **Add project** وأنشئ مشروعاً (مثلاً باسم `asr-alhadatha`).

## 2) تفعيل الخدمات المطلوبة (من لوحة Firebase)
- **Authentication** → Sign-in method → فعّل **Email/Password**.
- **Firestore Database** → Create database → ابدأ بوضع **Production** أو **Test**.
- **Storage** → Get started (لتخزين صور العمل).

## 3) تثبيت الأدوات (مرة واحدة على جهازك)
```bash
npm install -g firebase-tools
dart pub global activate flutterfire_cli
firebase login
```
> تأكد أن مسار `dart pub global` مضاف إلى متغيّر PATH.

## 4) ربط المشروع تلقائياً
من داخل مجلد المشروع `mohameed_app` نفّذ:
```bash
flutterfire configure
```
- اختر مشروع Firebase الذي أنشأته.
- اختر المنصّات (Android على الأقل).
- سيُولّد الأمر ملف `lib/firebase_options.dart` الصحيح (يستبدل الملف المؤقت)،
  وملف `android/app/google-services.json` تلقائياً.

## 5) التشغيل
```bash
flutter pub get
flutter run
```
إذا ظهرت شاشة «يلزم ضبط إعدادات Firebase أولاً» فهذا يعني أن الملف ما زال
بالقيم المؤقتة — أعد تنفيذ `flutterfire configure` ثم أعد التشغيل.

---

## 6) قواعد أمان Firestore (مبدئية)
من Firestore → Rules، الصق التالي ثم Publish (تسمح فقط للمستخدمين المسجّلين):
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```
> هذه قاعدة بسيطة للبداية. لاحقاً يُفضّل تقييد الكتابة حسب الدور.

## 7) قواعد أمان Storage (مبدئية)
من Storage → Rules:
```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

---

## 8) إنشاء الحسابات والأدوار
التطبيق فيه شاشة **إنشاء حساب**، وعند الإنشاء تختار الدور:
- **الإدارة** — تتابع التصميم وتضيف مواقع العمل للتنفيذ وترى الإشعارات.
- **المحاسبة** — ترى التصميم والتنفيذ وصفحة كل موظف وبصمته وميزانية كل زبون.
- **موظف التصميم** — البصمة + إدراج مشاريع التصميم. (اختر القسم: مدني/معماري)
- **موظف التنفيذ** — البصمة + مواقع العمل (دخول الموقع، تقارير، صور، سلف وصرفيات).
- **الزبون** — يرى مشاريعه وميزانيته بالكامل.

> أنشئ أولاً حساب **الإدارة**، ثم حسابات الموظفين والزبائن.
> لربط الزبون بمشاريعه: عند إضافة مشروع تصميم أو موقع عمل، اختر حساب الزبون من
> قائمة «ربط بحساب الزبون».

## 9) جدول البصمة (مثبّت في التطبيق)
- **المدني**: من 7 صباحاً إلى 3 عصراً (8 ساعات).
- **المعماري**: من 10 صباحاً إلى 6 مساءً (8 ساعات).
- **الجمعة** عطلة (يُمنع تسجيل الدخول فيها).
- يُحسب تلقائياً: عدد أيام الدوام، ساعات التغيّر (تأخير/انصراف مبكر)، والساعات الإضافية.

## 10) الشعار (اللوكو)
ضع صورة الشعار في المسار:
```
assets/images/logo.png
```
إن لم تضعها، سيرسم التطبيق شعاراً بديلاً بنفس الألوان تلقائياً.

---

### ملاحظات تقنية
- الاستعلامات تُرتّب داخل التطبيق، لذلك **لا تحتاج إلى إنشاء فهارس (indexes)** في Firestore.
- صلاحيات الموقع (GPS) مطلوبة للبصمة وتسجيل دخول المواقع — سيطلبها التطبيق عند أول استخدام.
- الحد الأدنى لإصدار أندرويد: 23 (مطلوب من Firebase Auth).
