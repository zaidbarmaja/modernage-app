<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- الإنترنت لقاعدة بيانات Firebase. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- الموقع «أثناء الاستخدام» فقط: يُطلب لحظة ضغط الموظف على زر تسجيل الحضور
         للتحقّق من وجوده في موقع العمل. لا يوجد أي تتبّع في الخلفية. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- إشعارات تأكيد تسجيل الحضور/الانصراف والتنبيهات المهمّة. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- البصمة/Face ID للتحقّق من هوية الموظف قبل تسجيل الحضور (local_auth). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- لإعادة جدولة تذكيرات الدوام اليومية بعد إعادة تشغيل الجهاز. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="عصر الحداثة"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- تعطيل Impeller لتفادي الشاشة السوداء على بعض محاكيات أندرويد
             (يستخدم محرك Skia المستقر بدلاً منه). -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- قناة الإشعارات الافتراضية لإشعارات FCM القادمة والتطبيق مغلق
             (عالية الأهمية مع صوت — نفس قناة التذكيرات). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="work_reminders" />
        <!-- مستقبِلات flutter_local_notifications: لعرض التذكيرات المجدولة وإعادة
             جدولتها بعد إعادة تشغيل الجهاز (تذكيرات الدوام اليومية). -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- لفتح الروابط وواتساب والاتصال (url_launcher) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
