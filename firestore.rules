rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // أي مستخدم مسجّل الدخول (حتى المجهول/Anonymous) يقرأ ويكتب.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
