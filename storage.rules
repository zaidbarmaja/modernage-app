rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // رفع/قراءة الصور للمستخدمين المسجّلين فقط.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
