# دليل صيانة تطبيق «عصر الحداثة»

دليل عملي: كل عطل ممكن يصير، سببه، وحلّه — مرتّب حسب الجزء المسؤول.
آخر تحديث: ٢٩ تموز ٢٠٢٦.

---

## ١) خريطة النظام — من يكلّم من

النظام أربعة أجزاء تشترك في **قاعدة بيانات واحدة** (مشروع Firebase اسمه `mohmeed-kadim`):

| الجزء | مكانه | وظيفته |
|---|---|---|
| تطبيق الهاتف (Flutter) | `lib/` | الموظف والزبون والمدير |
| لوحة التحكم (ويب) | `DASHBORD/` | إدارة المستخدمين والإشعارات والتقارير |
| تطبيق الويب الثانوي | `modage/` | الحسابات والمعاملات |
| الدالة السحابية | `functions/index.js` | تحويل الإشعارات إلى Push على الهواتف |

**القاعدة الذهبية:** أي شيء يُكتب في قاعدة البيانات من أي جزء، يراه بقية الأجزاء فوراً.
لا يوجد سيرفر خاص بنا — Firebase هو السيرفر.

```
لوحة التحكم ──┐
تطبيق الهاتف ──┼──> Firestore (قاعدة البيانات) ──> الدالة السحابية ──> FCM ──> هاتف المستخدم
تطبيق modage ──┘        mohmeed-kadim              pushOnNotification
```

---

## ٢) جدول الأعطال السريع

ابدأ من هنا. إذا لم تجد عطلك، اذهب للقسم المفصّل.

| العرَض | السبب الأرجح | الحل السريع |
|---|---|---|
| الإشعارات ما تصل الهاتف أبداً | المستخدم رافض إذن الإشعارات، أو ما سجّل دخول بعد تنصيب التطبيق | القسم ٣ |
| الإشعارات تصل لبعض الموظفين فقط | الباقون ما فتحوا التطبيق بعد آخر تحديث (ما انسجّل رمز جهازهم) | القسم ٣-ب |
| الإشعارات وقفت فجأة للجميع | الفوترة (Blaze) توقّفت، أو الدالة السحابية انحذفت | القسم ٤ |
| الإشعار يظهر بالتطبيق بس ما يوصل الهاتف | الإشعار بلا مستلِم (`toUid` فارغ) — إشعار إداري عام | مقصود، القسم ٣-ج |
| تذكيرات الدوام اليومية ما تشتغل | التذكير غير مفعّل بالداشبورد، أو فئته لا تطابق الموظف | القسم ٣-د |
| التطبيق يفتح شاشة «لا يوجد اتصال» | بوابة الاتصال — الجهاز فعلاً بلا إنترنت | طبيعي |
| المدير ما يكدر يدخل اللوحة | كلمة المرور تغيّرت بمكان وما تغيّرت بالثاني | القسم ٦ |
| «تعذّر تحميل البيانات» بكل الشاشات | قواعد الأمان أو المستخدم غير مسجّل دخول | القسم ٧ |
| البناء يفشل عند الإصدار | keystore ناقص أو `flutter clean` مطلوب | القسم ٨ |
| إشعارات iOS ما تشتغل نهائياً | مفتاح APNs غير مرفوع — غير مكتمل أصلاً | القسم ٩ |

---

## ٣) الإشعارات — أهم قسم

### كيف تشتغل السلسلة (خمس خطوات، أي واحدة تنكسر = ما يوصل إشعار)

```
١. المستخدم يفتح التطبيق ويسجّل دخول
   └─> Notifications.registerDevice(uid)  يحفظ رمز جهازه في users/{uid}.fcmTokens

٢. أحد يرسل إشعاراً (من الداشبورد أو من التطبيق)
   └─> يُكتب مستند جديد في مجموعة notifications فيه toUid = المستلِم

٣. الدالة السحابية pushOnNotification تصحى فوراً على هذا المستند

٤. تقرأ users/{toUid}.fcmTokens وترسل لكل أجهزته عبر FCM

٥. هاتف المستخدم يستقبل ويعرض الإشعار (قناة work_reminders، صوت افتراضي)
```

**كل كود الإشعارات في ملف واحد:** [`lib/services/notifications.dart`](lib/services/notifications.dart).
في أوّل الملف فهرس مكتوب بكل إشعار يخرج من النظام ومن أين ولمن. لا تُضِف إشعارات في أي ملف آخر.

### أ) الإشعارات ما تصل مستخدماً معيّناً

افحص بالترتيب:

1. **إذن الإشعارات على جهازه** — إعدادات الهاتف ← التطبيقات ← عصر الحداثة ← الإشعارات: لازم مفعّلة.
   على أندرويد ١٣ فما فوق، الإذن يُطلب أول مرة فقط؛ إذا رفضه المستخدم لا يُطلب ثانية.
2. **هل رمز جهازه محفوظ؟** — افتح Firebase Console ← Firestore ← `users` ← مستنده ← لازم يوجد حقل
   `fcmTokens` وفيه نص طويل واحد على الأقل.
   - إذا الحقل غير موجود: خلّه يخرج من التطبيق ويدخل من جديد (التسجيل يصير عند الدخول فقط).
3. **هل الإشعار موجّه له؟** — في مجموعة `notifications`، حقل `toUid` لازم يساوي `uid` حسابه بالضبط.
4. **موفّر البطارية** — بعض هواتف شاومي/هواوي توقف الإشعارات للتطبيقات «المقيّدة».
   إعدادات الهاتف ← البطارية ← استثناء التطبيق من التقييد.

### ب) وصلت لبعض الموظفين فقط

هذا يعني أن الباقين لم يسجّلوا رمز أجهزتهم بعد. **رمز الجهاز يُسجَّل عند تسجيل الدخول فقط.**
الحل: اطلب منهم فتح التطبيق وتسجيل الدخول مرّة واحدة.

ملاحظة: الرموز الميّتة (جهاز انحذف منه التطبيق) تُنظَّف تلقائياً — الدالة السحابية تحذفها من
`fcmTokens` بنفسها. لا تحتاج تدخّل.

### ج) الإشعار يظهر داخل التطبيق لكن لا يصل الهاتف

**هذا سلوك مقصود.** الإشعار الذي `toUid` فيه فارغ = إشعار إداري عام، يظهر في تبويب الإشعارات
داخل التطبيق فقط ولا يُرسَل Push (لأنه بلا مستلِم محدّد).
إذا أردته أن يصل الهواتف، أرسله من زر «إرسال إشعار للجميع» — هذا يُنشئ إشعاراً مستقلاً لكل مستخدم.

### د) تذكيرات الدوام اليومية لا تظهر

هذه **محلية على الجهاز** — لا علاقة لها بالإنترنت ولا بالدالة السحابية.

1. افتح الداشبورد ← الإشعارات ← تأكد أن التذكير **مفعّل** (`enabled`).
2. تأكد أن **الفئة المستهدفة** تطابق الموظف: `all` / `execution` / `design` / `pools` / `customers`.
3. الجدولة تتم عند **تسجيل الدخول**. بعد أي تعديل بالداشبورد، الموظف لازم يفتح التطبيق مرّة.
4. الحد الأقصى ٦٠ تذكيراً مفعّلاً؛ ما زاد يُتجاهَل.
5. المنطقة الزمنية: إذا تعذّر تحديدها يستخدم التطبيق **بغداد** تلقائياً.

### هـ) اختبار سريع للسلسلة كاملة

من الداشبورد أرسل إشعاراً لمستخدم واحد (أنت مثلاً)، ثم:

```bash
firebase functions:log --only pushOnNotification --project mohmeed-kadim
```

- ظهر سطر تنفيذ بلا خطأ + وصل الإشعار → السلسلة سليمة.
- ظهر سطر لكن ما وصل → المشكلة بالجهاز (إذن/بطارية/رمز غير محفوظ).
- ما ظهر أي سطر → المشكلة بالدالة نفسها ← القسم ٤.

---

## ٤) الدالة السحابية

**اسمها:** `pushOnNotification` — **مكانها:** `functions/index.js` — **المشروع:** `mohmeed-kadim`
**المنطقة:** us-central1 — **الإصدار:** الجيل الثاني، nodejs20.

### الأوامر (كلها تُنفَّذ من مجلد المشروع)

```bash
# هل الدالة منشورة أصلاً؟
firebase functions:list --project mohmeed-kadim

# سجلّاتها (لمعرفة سبب أي فشل)
firebase functions:log --only pushOnNotification --project mohmeed-kadim

# إعادة نشرها بعد أي تعديل، أو لإرجاعها إذا انحذفت
firebase deploy --only functions --project mohmeed-kadim

# إذا انحذف مجلد node_modules
cd functions && npm install && cd ..
```

### الأعطال المحتملة

| العطل | العلامة | الحل |
|---|---|---|
| الفوترة توقّفت | كل الإشعارات وقفت دفعة واحدة | فعّل خطة **Blaze** في Firebase Console ← الفوترة |
| الدالة انحذفت | `functions:list` لا يعرضها | `firebase deploy --only functions` |
| النشر يفشل | خطأ npm | `cd functions && npm install` ثم أعد النشر |
| تعديل بالكود ما ظهر أثره | نُسيت إعادة النشر | الدالة **لا تتحدّث تلقائياً** — لازم `firebase deploy` |

**مهم:** تعديل `functions/index.js` على جهازك **لا يغيّر شيئاً** حتى تنشره. النسخة العاملة هي
المنشورة على السيرفر، وليست الملف عندك.

**التكلفة:** خطة Blaze مطلوبة، لكن الاستخدام ضمن الحد المجاني عملياً. سياسة تنظيف الحِزَم
مضبوطة على يوم واحد لتقليل تكلفة التخزين.

---

## ٥) ماذا لو أردت إضافة إشعار جديد

كل شيء في [`lib/services/notifications.dart`](lib/services/notifications.dart):

1. أضف دالة جديدة بجانب `newReport` / `newReceipt` (انسخ واحدة وعدّلها).
2. حدّد `toUid` — بدونه لن يصل Push.
3. نادِها من الشاشة التي يحصل فيها الحدث.
4. أضف سطراً في الفهرس أعلى الملف.
5. **لا تحتاج تعديل الدالة السحابية ولا إعادة نشرها** — أي مستند جديد في `notifications` يُرسَل تلقائياً.

لإضافة أيقونة لنوع جديد: `lib/widgets/notifications_view.dart` ← الدالة `_iconFor`.

---

## ٦) كلمات المرور — كيف أغيّرها

### أ) كلمة مرور المدير (الأهم)

اسم الدخول: **`mo_3g`** — البريد الداخلي للمصادقة: **`mo_3g@asr-app.com`**

كلمة المرور مخزّنة في **مكانين لازم يتطابقان**:

| الجهة | المكان | ملاحظة |
|---|---|---|
| Firebase Auth | Firebase Console (السيرفر) | **المصدر الحقيقي** — هي التي تتحقّق فعلياً |
| لوحة التحكم | `DASHBORD/js/config.js` سطر ٩ | مكتوبة بالنصّ الصريح |
| تطبيق modage | `modage/js/config.js` سطر ٨ | مكتوبة بالنصّ الصريح |
| تطبيق الهاتف | ❌ غير مخزّنة | المدير يكتبها يدوياً — آمن |

**خطوات التغيير (نفّذها بهذا الترتيب، ولا تترك أي خطوة):**

1. افتح [Firebase Console](https://console.firebase.google.com/project/mohmeed-kadim/authentication/users)
   ← **Authentication** ← **Users**.
2. ابحث عن `mo_3g@asr-app.com` ← النقاط الثلاث ← **Reset password** (أو عيّن كلمة جديدة).
3. افتح `DASHBORD/js/config.js` وغيّر السطر:
   ```js
   password: "الكلمة_الجديدة_هنا",
   ```
4. افتح `modage/js/config.js` وغيّر نفس السطر **بنفس القيمة بالضبط**.
   القيمتان لازم تتطابقان مع Firebase Auth، وإلا يتوقّف أحد التطبيقين عن الدخول.
5. احفظ وأعد تحميل صفحة اللوحة (Ctrl+F5).
6. إذا كان `modage` منشوراً على Firebase Hosting، أعد نشره ليأخذ الكلمة الجديدة:
   ```bash
   firebase deploy --only hosting --project mohmeed-kadim
   ```
7. **تطبيق الهاتف لا يحتاج أي تعديل ولا إعادة بناء** — اكتب الكلمة الجديدة في شاشة الدخول وبس.

> ⚠️ **تنبيه أمني مهم:** كلمة مرور المدير مكتوبة صراحةً في **ملفَّين**
> (`DASHBORD/js/config.js` و`modage/js/config.js`)، وقد شُحنت ضمن نسخ سابقة. أي ملف
> JavaScript على موقع منشور يقدر أي زائر يفتحه ويقرأه من متصفّحه — وقد تُحقّق فعلاً
> أن الملف مقروء للعموم على `mohmeed-kadim.web.app/js/config.js`.
> **غيّرها في أقرب وقت** بالخطوات أعلاه.
>
> لا تكتب الكلمة الفعلية في هذا الملف ولا في أي ملف يُرفع على مستودع عام.
>
> ولا ترفع مجلد `DASHBORD` على استضافة عامة وفيه كلمة المرور.

### ب) كلمات مرور الموظفين والزبائن

هذه تُدار **بالكامل من لوحة التحكم**، بلا لمس أي كود:

1. افتح اللوحة ← صفحة **المستخدمين**.
2. عند اسم الموظف اضغط زر **«كلمة المرور»**.
3. اكتب الرمز الجديد (رمز الموظف عادةً ٤ أرقام) ← حفظ.

الرمز يُحفظ **مجزّأً (hash + salt)** في مجموعة `credentials` منفصلة عن ملف المستخدم — الطاقم
لا يقدر يقرأ كلمات مرور بعضهم. تفعيل هذا المسار يتم تلقائياً عند أول تعيين
(`useFirestorePassword = true`).

الموظف الجديد: صفحة المستخدمين ← إضافة ← يُطلب منك «رمز الدخول (٤ أرقام)»، وهذا هو رمزه.

### ج) نسيت كل شيء وما تكدر تدخل

الحساب موجود في Firebase Auth. ادخل على
[Firebase Console](https://console.firebase.google.com/project/mohmeed-kadim/authentication/users)
بحساب Google المالك للمشروع، وعيّن كلمة مرور جديدة لـ `mo_3g@asr-app.com`، ثم حدّث
`DASHBORD/js/config.js`. الوصول لـ Console هو مفتاح الأمان الأخير — احمِ حساب Google هذا
بالتحقّق بخطوتين.

---

## ٧) قاعدة البيانات والصلاحيات

**ملف القواعد:** `firestore.rules` — **النموذج الحالي:** أي مستخدم **مسجّل دخول** يقرأ ويكتب
على كل المجموعات، عدا `auditLog` (إنشاء فقط، لا تعديل ولا حذف). غير المسجّل: لا شيء.

```bash
# نشر القواعد بعد أي تعديل
firebase deploy --only firestore:rules --project mohmeed-kadim
```

| العطل | السبب | الحل |
|---|---|---|
| «تعذّر تحميل البيانات» في كل الشاشات | المستخدم غير مسجّل دخول | خروج ثم دخول |
| `permission-denied` بالسجلات | القواعد المنشورة أقدم من الملف | أعد نشر القواعد |
| بيانات قديمة تظهر بالتطبيق | الكاش المحلي مفعّل (مقصود لتقليل التكلفة) | اسحب للتحديث، أو أغلق التطبيق وافتحه |

**المجموعات المهمة:** `users` (وفيها `fcmTokens`) — `notifications` — `scheduled_reminders` —
`credentials` — `attendance` — `settings/company`.

---

## ٨) البناء والإصدار

```bash
# تنظيف عند أي خطأ بناء غريب
flutter clean && flutter pub get

# فحص الكود قبل أي إصدار (لازم يطلع بلا أخطاء)
flutter analyze

# نسخة تجريبية
flutter build apk --release

# نسخة المتجر
flutter build appbundle --release
```

**مفاتيح التوقيع:**

- `android/app/upload-keystore.jks` + `android/key.properties`
- معرّف التطبيق: `modernage.online`

> 🔴 **لا تفقد ملف `upload-keystore.jks` ولا كلمة مروره أبداً.** بدونه لا تستطيع رفع أي تحديث
> لنفس التطبيق على Google Play إلى الأبد. خذ منه نسخة احتياطية في مكان آمن خارج الجهاز.

**قبل كل إصدار:** ارفع رقم الإصدار في `pubspec.yaml` (السطر `version:`) — الجزء بعد `+`
(رقم البناء) لازم يزيد، وإلا يرفض المتجر الرفع.

---

## ٩) iOS — غير مكتمل

إشعارات iOS **لا تعمل حالياً**. الناقص:

1. **مفتاح APNs**: من حساب مطوّر آبل، أنشئ APNs Authentication Key (ملف `.p8`) وارفعه في
   Firebase Console ← إعدادات المشروع ← Cloud Messaging.
2. **قدرات Xcode**: فعّل `Push Notifications` و`Background Modes` ← `Remote notifications`،
   مع ملف تعريف (provisioning profile) يدعم Push.

(`remote-notification` موجود أصلاً في `ios/Runner/Info.plist`.)

أندرويد يعمل بلا أي إعداد إضافي — `google-services.json` موجود.

---

## ١٠) الشعار والهوية

| الملف | الاستخدام |
|---|---|
| `brand/logo.svg` | الشعار الكامل للخلفيات الفاتحة |
| `brand/logo_on_dark.svg` | الشعار الكامل للخلفية الداكنة (داخل التطبيق) |
| `brand/logo_mark.svg` | الرمز وحده بلا اسم |
| `brand/logo*.png` | نسخ جاهزة عالية الدقة بخلفية شفافة |
| `brand/logo.template.svg` | **القالب** — عدّل هنا فقط |

```bash
dart run tool/make_brand.dart   # يعيد توليد ملفات SVG من القالب
dart run tool/make_icon.dart    # يعيد توليد أيقونة التطبيق من assets/images/logo.png
```

ألوان الهوية (من `lib/core/theme.dart`): زيتوني `#7C8A57` — زيتوني داكن `#515E39` —
زيتوني فاتح `#A8BA82` — كريمي `#ECEFE1` — خلفية `#161911`.

---

## ١١) صيانة دورية

| كل | افعل |
|---|---|
| أسبوع | افتح الداشبورد وتأكد أن الأرقام تتحدّث |
| شهر | `firebase functions:log` — تأكد بلا أخطاء متكرّرة |
| شهر | راجع فاتورة Firebase (Blaze) — لازم تبقى ضمن المجاني |
| ٣ أشهر | `flutter pub outdated` ثم حدّث الحزم بحذر واختبر |
| عند كل إصدار | `flutter analyze` + اختبار إشعار حقيقي على هاتف |

**ملفات لا تُحذف ولا تُشارك:**
`android/app/upload-keystore.jks` — `android/key.properties` —
`android/app/google-services.json` — `DASHBORD/js/config.js` — `lib/firebase_options.dart`

---

## ١٢) أين أجد ماذا

| تريد تعديل | الملف |
|---|---|
| أي إشعار في النظام | `lib/services/notifications.dart` |
| إرسال Push من السيرفر | `functions/index.js` |
| دخول المدير (التطبيق) | `lib/core/app_login.dart` |
| دخول المدير (اللوحة) + كلمة المرور | `DASHBORD/js/config.js` |
| دخول المدير (modage) + كلمة المرور | `modage/js/config.js` |
| الألوان والثيم | `lib/core/theme.dart` |
| أسماء مجموعات قاعدة البيانات | `lib/core/constants.dart` |
| كل قراءة/كتابة لقاعدة البيانات (التطبيق) | `lib/services/firestore_service.dart` |
| كل قراءة/كتابة (اللوحة) | `DASHBORD/js/data.js` |
| قواعد الأمان | `firestore.rules` |
| صلاحيات أندرويد وقناة الإشعارات | `android/app/src/main/AndroidManifest.xml` |
